#includebitsstdc++.h
using namespace std;

void MAx(int A[],int n){
	
	sort(A,A+n);
	int max = A[0] - A[n-1];
	max = abs(max);
	coutmaxn;
		
}

int main(){
	ios_basesync_with_stdio(false);
    cin.tie(NULL);
	int t ;
	 cint;
	 
	 while(t--){
	 	int n;
	 	cinn;
	 	int A[n];
	 	for(int i=0;in;i++)	cinA[i];
	 	MAx(A,n);
	 }
}